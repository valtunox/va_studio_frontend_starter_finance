# name: Deploy to AWS CloudFront

# on:
#   push:
#     branches:
#       - main
#       - production
#       - master
#       - dev
#   workflow_dispatch:

# env:
#   NODE_VERSION: '20'
#   AWS_REGION: 'us-east-1'
#   S3_BUCKET_URL: 's3://devrecruitagent.com'
#   CLOUDFRONT_DISTRIBUTION_ID: 'E2MUA4CDGYQAXN'
#   CLOUDFRONT_DOMAIN: 'd3qbao5sh7zc4o.cloudfront.net'

# jobs:
#   deploy:
#     name: Build and Deploy to CloudFront
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci --force

#       - name: Build Next.js application
#         env:
#           NODE_ENV: production
#           NEXT_TELEMETRY_DISABLED: 1
#         run: npm run build

#       - name: Export static files (if using static export)
#         run: npm run export || echo "No export script found, skipping..."
#         continue-on-error: true

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Verify AWS identity
#         run: aws sts get-caller-identity

#       - name: Sync files to S3 bucket
#         run: |
#           if [ -d "out" ]; then
#             # If using static export
#             aws s3 sync out/ ${{ env.S3_BUCKET_URL }} \
#               --delete \
#               --cache-control "public, max-age=31536000, immutable" \
#               --region ${{ env.AWS_REGION }} \
#               --exclude "*.html" \
#               --exclude "*.json"
            
#             # Upload HTML and JSON with different cache settings
#             aws s3 sync out/ ${{ env.S3_BUCKET_URL }} \
#               --exclude "*" \
#               --include "*.html" \
#               --include "*.json" \
#               --region ${{ env.AWS_REGION }} \
#               --cache-control "public, max-age=0, must-revalidate"
#           else
#             # If using server build (upload .next folder)
#             aws s3 sync .next/ ${{ env.S3_BUCKET_URL }}/.next \
#               --delete \
#               --region ${{ env.AWS_REGION }} \
#               --cache-control "public, max-age=31536000, immutable"
            
#             aws s3 sync public/ ${{ env.S3_BUCKET_URL }}/public \
#               --delete \
#               --region ${{ env.AWS_REGION }} \
#               --cache-control "public, max-age=31536000, immutable"
#           fi

#       - name: Invalidate CloudFront distribution
#         run: |
#           aws cloudfront create-invalidation \
#             --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
#             --paths "/*"

#       - name: Get CloudFront distribution URL
#         run: |
#           echo "🚀 Deployment successful!"
#           echo "📍 CloudFront URL: https://${{ env.CLOUDFRONT_DOMAIN }}"

#       - name: Deployment Summary
#         run: |
#           echo "## 🎉 Deployment Successful" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "**Environment:** AWS CloudFront" >> $GITHUB_STEP_SUMMARY
#           echo "**Region:** ${{ env.AWS_REGION }}" >> $GITHUB_STEP_SUMMARY
#           echo "**S3 Bucket:** ${{ env.S3_BUCKET_URL }}" >> $GITHUB_STEP_SUMMARY
#           echo "**CloudFront Distribution:** ${{ env.CLOUDFRONT_DISTRIBUTION_ID }}" >> $GITHUB_STEP_SUMMARY
#           echo "**CloudFront Domain:** https://${{ env.CLOUDFRONT_DOMAIN }}" >> $GITHUB_STEP_SUMMARY
#           echo "**Deployed at:** $(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY

#   verify:
#     name: Verify Deployment
#     runs-on: ubuntu-latest
#     needs: deploy
    
#     steps:
#       - name: Wait for CloudFront propagation
#         run: sleep 30

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Set CloudFront URL
#         id: get-url
#         run: |
#           echo "url=https://${{ env.CLOUDFRONT_DOMAIN }}" >> $GITHUB_OUTPUT

#       - name: Check website health
#         run: |
#           HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" ${{ steps.get-url.outputs.url }})
#           if [ "$HTTP_CODE" -eq 200 ]; then
#             echo "✅ Website is responding with HTTP 200"
#           else
#             echo "❌ Website returned HTTP $HTTP_CODE"
#             exit 1
#           fi

#       - name: Verification Summary
#         if: success()
#         run: |
#           echo "## ✅ Verification Passed" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "Your website is live and responding correctly!" >> $GITHUB_STEP_SUMMARY
#           echo "" >> $GITHUB_STEP_SUMMARY
#           echo "🌐 **Visit:** ${{ steps.get-url.outputs.url }}" >> $GITHUB_STEP_SUMMARY

